import { SubscriptionsDirective } from './subscriptions.directive';

describe('SubscriptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscriptionsDirective();
    expect(directive).toBeTruthy();
  });
});
